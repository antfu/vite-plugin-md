import { describe, expect, it } from 'vitest'
// import Simple, { title } from './fixtures/simple.md'

describe('component tests', () => {
  it.skip('importing a markdown file\'s default export gives us a VueJS component', () => {
    // expect(Simple).toBeTypeOf('object')
  })

  it.skip('frontmatter properties can be imported from a markdown file', () => {
    // expect(title).toBe('Hello World')
  })
})
